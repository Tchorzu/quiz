import styled from "styled-components";

export const StyledContainer = styled.div`
  margin: 20px;
  padding: 20px;
  border: 1px solid var(--var-border);
  border-radius: 10px;
  background-color: var(--var-bg-quiz);
  -webkit-box-shadow: var(--var-box-shadow);
  -moz-box-shadow: var(--var-box-shadow);
  box-shadow: var(--var-box-shadow);
`;
